name: Setup Kubernetes with kubeadm
description: Installs containerd, kubeadm, kubelet, and kubectl

runs:
  using: "composite"
  steps:
    - name: Install and configure Kubernetes prerequisites
      shell: bash
      run: |
        set -e

        echo "[1/10] Updating system and disabling swap"
        sudo apt update && sudo apt upgrade -y
        sudo swapoff -a
        sudo sed -i '/ swap / s/^/#/' /etc/fstab

        echo "[2/10] Loading kernel modules"
        cat <<EOF | sudo tee /etc/modules-load.d/k8s.conf
overlay
br_netfilter
EOF
        sudo modprobe overlay
        sudo modprobe br_netfilter

        echo "[3/10] Applying sysctl settings"
        cat <<EOF | sudo tee /etc/sysctl.d/k8s.conf
net.bridge.bridge-nf-call-ip6tables = 1
net.bridge.bridge-nf-call-iptables = 1
net.ipv4.ip_forward = 1
EOF
        sudo sysctl --system

        echo "[4/10] Installing containerd runtime"
        sudo apt install -y containerd

        echo "[5/10] Configuring containerd with systemd cgroup"
        sudo mkdir -p /etc/containerd
        containerd config default | sudo tee /etc/containerd/config.toml > /dev/null
        sudo sed -i 's/SystemdCgroup = false/SystemdCgroup = true/' /etc/containerd/config.toml
        sudo systemctl restart containerd
        sudo systemctl enable containerd

        echo "[6/10] Installing APT transport packages"
        sudo apt install -y apt-transport-https ca-certificates curl gpg

        echo "[7/10] Adding Kubernetes repo and key"
        curl -fsSL https://pkgs.k8s.io/core:/stable:/v1.29/deb/Release.key | sudo gpg --dearmor -o /etc/apt/trusted.gpg.d/kubernetes-apt-keyring.gpg
        echo "deb [signed-by=/etc/apt/trusted.gpg.d/kubernetes-apt-keyring.gpg] https://pkgs.k8s.io/core:/stable:/v1.29/deb/ /" | sudo tee /etc/apt/sources.list.d/kubernetes.list

        echo "[8/10] Installing Kubernetes components"
        sudo apt update
        sudo apt install -y kubelet kubeadm kubectl

        echo "[9/10] Holding kubelet, kubeadm, kubectl at current versions"
        sudo apt-mark hold kubelet kubeadm kubectl

        echo "[10/10] Kubernetes setup completed"

